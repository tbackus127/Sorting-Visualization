/*
public class SelectionSort extends GSortMember {
    
	public SelectionSort(ArrayMember[] argArray) {
		super();
	}
	
    public void sort() {
	
		for(int i = 0; i < arr.length; i++) {
			int selection = 0;
			int comp = arr.getValue(i);
			for(int j = i+1; j < arr.length; j++) {
			int curr = arr.getValue(j);
			if(curr < comp)
				arr.SWAP(i, j);
			}
		}
	}
}
*/