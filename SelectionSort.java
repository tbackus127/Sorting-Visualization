/*
public class SelectionSort extends GSortMember implements GSort {
    
    public void sort() {
	
	for(int i = 0; i < (ARRAY.SIZE); i++) {
	    int selection = 0;
	    int comp = ARRAY.GET(i);
	    for(int j = i+1; j < (ARRAY.SIZE); j++) {
		int curr = ARRAY.GET(j);
		if(curr < comp)
		    ARRAY.SWAP(i, j);
		}
	    }
	}
	
}
*/